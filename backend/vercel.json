{
    "version": 2,
    "builds": [
      { "src": "api/**/*.ts", "use": "@vercel/node" }
    ],
    "includeFiles": ["../shared-types/**/*"],
    "ignoreCommand": "echo 'ignoring files not in workspaces'"
  }
  